The goal is to read a set of x, y coordinates and draw a scatter plot with the corresponding values. 
The input file is an HTML table which has 3 columns; x, y coordinate and Character. The character is in unicode. 
The challenge was to check the formatting.

At the very end, after the plotting, the plot should reveal a message written in English. The final output is in HTMLmsgPlot.png
